# Assignment 4 - Provide a Database API via PHP

## Setup

Copy the files `api.php` and `config.php` to `C:\xampp\htdocs`

## Usage

Open the URL http://localhost/api.php?tabelle=TABLE, where `TABLE` can be `bestellung`, `warenkorb`, `kunden`, `produkte`, or `hersteller`.
